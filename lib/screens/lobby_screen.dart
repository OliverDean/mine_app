import 'package:flutter/material.dart';
import '../services/game_service.dart';

class LobbyScreen extends StatelessWidget {
  // ...

  @override
  Widget build(BuildContext context) {
    // Display list of available games and allow user to join
  }
}
